extends layout

block content
  h1= title
  p Welcome to #{title}
    ul
    each todo in todos
      li
        = todo.task_name
        | (
        a(href="/todos/destroy/" + todo.id) Done
        | )
    | Creating a new task:
    form(action="/todos/create", method="post", style="display: inline")
      input(type="text", name="task_name")
